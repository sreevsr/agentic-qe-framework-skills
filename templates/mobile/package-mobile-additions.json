{
  "_comment": "These dependencies are merged into output/package.json when setup-mobile-framework skill runs. Do not use this file as a standalone package.json.",
  "dependencies": {
    "@wdio/cli": "^9.0.0",
    "@wdio/local-runner": "^9.0.0",
    "@wdio/mocha-framework": "^9.0.0",
    "@wdio/spec-reporter": "^9.0.0",
    "@wdio/json-reporter": "^9.0.0",
    "@wdio/types": "^9.0.0",
    "webdriverio": "^9.0.0",
    "expect-webdriverio": "^5.0.0"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.0"
  },
  "scripts": {
    "test:mobile:android": "npx wdio wdio.conf.ts --spec",
    "test:mobile:ios": "PLATFORM=ios npx wdio wdio.conf.ts --spec"
  }
}
